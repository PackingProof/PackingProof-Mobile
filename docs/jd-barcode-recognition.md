# 京东双条码识别约定

电脑端与手机端使用同一规则：复用当前帧查找，不为京东额外等待下一帧。

## 规则

- 首先按各端已有候选排序选择有效条码（电脑端中心距离优先，手机端面积优先）
- 首选为京东裸号时，在同帧其余有效候选中寻找同一运单的合法包裹号，多个匹配继续使用原排序
- 单包裹 `-1-1-` 与多包裹 `-1-2-`、`-2-2-` 都保留完整包裹号，作为录像订单号
- 首选已经是包裹号时直接处理；没有对应包裹号时返回裸号
- 桌面快速解码只得到裸号时继续当前图像的已有慢速解码；不切换到无关订单
- 后缀必须包含末尾横线，序号和总数为无前导零的正整数，序号不大于总数，数值不超过 2147483647
- 当前只启用已确认的 JD + 数字、JDVA + 数字，裸号长度 8–40，完整包裹号不超过 40；未确认前缀保持原样，现有用户单号校验仍生效
- 在稳定识别、去重和录制决策之前输出最终单号；单包裹的两种码不能绕过既有重复触发保护
- 无图像输入也保留完整后缀，不执行同帧查找
- 裸号与对应包裹号在同码停录及防重复触发中视为关联码，两个不同完整包裹号仍是不同录像身份
- 同码停录时如果首次找到对应包裹号，可将当前裸号记录补全后停止，不新建录像、不覆盖已确定的包裹号
- 录像查询复用包含搜索，裸号可查询多个包裹；桌面精确查询也支持裸号关联。商品信息查询使用裸运单号，录像和上传仍保留包裹号

## 边界

不额外等待下一帧，不迁移历史记录或修改上传协议。仅允许在停录时补全当前尚未结束的裸号记录。电脑端保持原文件路径，手机端沿用最终保存时命名；已经录入画面的水印不回写。

## 官方依据

- [模板编码表](https://open.jdl.com/#/open-business-document/access-guide/157/54628)
- [76x130 模板源码](https://template-content.jd.com/template-oss?tempCode=jdkd76x130)：Base64 解码后，两条 CODE128 分别绑定 packageCode 和 waybillCode
- [一单多包裹编号](https://open.jdl.com/#/question-docs/e6W15PGUR4/4rxaZbv8GT)

## 回归

两端 JdBarcodePolicy 测试保留相同的归一化样例，覆盖 JD/JDVA、单包裹、多包裹、缺少末尾横线、非法序号、溢出及非京东单号。另测同帧裸号优先、包裹号优先、无匹配回退、不同运单不配对、不同包裹不合并及持续可见不重复确认。

# 发布所需的本机配置模板。复制为 .env 后填写真实值：
#
#   cp .env.example .env
#
# .env 已被 .gitignore 忽略，只存在于本机，禁止提交。
# 这里只放路径、ID 和发布令牌，真正的密钥文件（keystore、.p8）一律留在仓库外。
#
# 填好后先跑 ./Tools/Check-ReleasePrereqs.sh 自检，再开始发布。
# 完整发布顺序见 docs/android-release.md，iOS 细节见 docs/ios-development.md。

# —— Android 正式签名（只有 Windows 编译机需要）——
# 指向仓库外的签名目录，里面放 keystore 与签名凭据。
# 被 Tools/Publish-Android.ps1 和 Tools/Build-Release-Diagnostic.ps1 读取。
PACKING_PROOF_SIGNING_DIRECTORY=

# —— iOS TestFlight 上传（只有 Mac 需要）——
# App Store Connect → 用户和访问 → 集成 → App Store Connect API 创建 API Key。
# Issuer ID 在同一页面顶部，注意它不是 Team ID。
# 被 Tools/Upload-TestFlight.sh 读取。
APP_STORE_CONNECT_KEY_ID=
APP_STORE_CONNECT_ISSUER_ID=
# AuthKey_<KEY_ID>.p8 的仓库外绝对路径。
# 留空时按 altool 约定在 ~/.appstoreconnect/private_keys/ 下找，
# 但密钥常常并不放在那里，所以建议显式填绝对路径。
APP_STORE_CONNECT_KEY_PATH=

# 没有 API Key 时的退路，与上面三项二选一。
# 专用密码可写成 @keychain:<item-name> 避免明文落盘。
# APPLE_ID=
# APPLE_APP_SPECIFIC_PASSWORD=

# —— 发布渠道令牌（两台机器各自填写）——
# Gitee 私人令牌，在 Gitee → 设置 → 私人令牌生成，只勾选发布所需权限。
# 被 Tools/Check-ReleasePrereqs.sh 与 Tools/Publish-Releases.sh 读取后导出给 gitee CLI。
# gitee CLI 自己保存的登录态按身份字符串各存一份，容易停在失效的旧身份上，
# 而且 `gitee auth status` 在令牌失效时仍然返回 0，所以这里显式提供令牌。
GITEE_TOKEN=

# —— 找不到真实值时 ——
# 这些凭据不在仓库里，也不该在。它们属于持有发布权限的人：
# Android 签名目录与 keystore、App Store Connect 的 API Key 及 .p8 文件。
# 换人接手时必须由上一位持有者移交，或在 App Store Connect 重新签发 API Key
# （旧 Key 可与新 Key 并存，不影响已上传的构建）。
# 不要试图从 shell 历史、会话记录或其他缓存里翻找这些值。
